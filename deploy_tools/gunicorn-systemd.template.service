[Unit]
Description=Gunicorn server for DOMAIN

[Service]
Restart=on-failure
User=maxd
WorkingDirectory=/home/maxd/sites/DOMAIN
EnvironmentFile=/home/maxd/sites/DOMAIN/.env

ExecStart=/home/maxd/sites/DOMAIN/virtualenv/bin/gunicorn \
    --bind unix:/tmp/DOMAIN.socket \
    superlists.wsgi:application

[Install]
WantedBy=multi-user.target
